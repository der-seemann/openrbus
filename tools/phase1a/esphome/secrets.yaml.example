# Copy to secrets.yaml locally and fill with deployment-specific values.
# Do not commit the resulting secrets.yaml.
api_encryption_key: "REPLACE_WITH_BASE64_API_KEY"
ota_password: "REPLACE_WITH_OTA_PASSWORD"
wifi_ssid: "REPLACE_WITH_WIFI_SSID"
wifi_password: "REPLACE_WITH_WIFI_PASSWORD"
fallback_ap_password: "REPLACE_WITH_FALLBACK_AP_PASSWORD"
